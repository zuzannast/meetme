%ul
  %li= link_to 'MeetMe', root_path
  - if user_signed_in?
    %li= link_to 'My profile', user_profile_path(current_user)
    %li= link_to 'Log out', destroy_user_session_path
  - else
    %li= link_to 'Log in', new_user_session_path
